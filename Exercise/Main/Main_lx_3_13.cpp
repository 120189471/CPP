#include<vector>
using std::vector;

#include<string>
using std::string;

#include<iostream>
using std::cout;
using std::endl;
using std::cin;

int main_lx_3_13(){
    /*
    vector<int> v1;
    vector<int> v2(10);
    vector<int> v3(10, 24);
    vector<int> v4{ 10 };
    vector<int> v5{ 10, 42 };
    vector<string> v6(10);
    vector<string> v7(10, "hi");

    for (auto i : v2){
        cout << i << endl;
    }
    cout << "------------------------------" << endl;
    for (int i = 0; i < v2.size(); i++){
        cout << v2[2] << endl;
    }
    */
    /*
    //这段有问题
    vector<string> vs;
    while (cin >> vs.push_back);
    
    for (auto &c : vs){
        //c=toupper(c);
    }
    */

    return 0;

}