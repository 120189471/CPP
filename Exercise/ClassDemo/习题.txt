习题一

	按要求修改GradeBook类

	a.包括第二个string数据成员，他表示授课老师的名字。
	b.提孔一个可以改变老师姓名的设置函数，以及一个得到该函数的获取函数
	c.修改构造函数，他指定了两个形参，一个是针对课程名称的，一个是针对教师姓名
	d.修改成员函数displayMessage，是的他先显示函数的欢迎信息和课程名称，然后输出“This Course is presented by :”然后输出老师的姓名。

习题二
	创建一个Account的类，这个类包括一个数据类型为int的数据成员，表示账户余额
	这个类必须提供一个构造函数，他接受初始余额并用他初始化数据成员
	有效性，要么大于等于0，否则设置为0，并且构造函数现实一条错误信息，指出初始余额无效，
	该类的还需提供3个成员函数，
	成员函数credit,将一笔金额加到当前的余额中
	debit,从这个Account中取钱，并保证取出金额不超过Account的余额，如果不是这样，余额不变，并且打印出一条信息，指出“”Debit
	getBalance，将返回余额